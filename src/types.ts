export type Location = { entranceObjects?:{id:string;name?:string;x:number;y:number}[]; notificationChunks?:number[]; x:number; y:number; region:number; plane:number|null; mapId:string|null; role:string; caption:string; source:string; title:string; revision:number|null; verified:boolean;outline?:[number,number][][];pinX?:number;pinY?:number;icon?:string|null;mtype?:string|null;zoom?:number|null;markerSource?:string;tiles?:{version:string;mapId:number;plane:number} };
export type EncounterMetadata = {canonicalId:string;recommendedSeconds:number;petIcon:string};
export type Boss = { metadataBase?:EncounterMetadata;recommendedSeconds?:number|null;petIcon?:string; entranceEntry?:Boss; id:string; name:string; regions:number[]; deathType:string; regionType?:string; raw:string|null; optionalArgs:string[]; maps:Location[]; warnings:string[]; links:string[]; wikiTitle:string; area?:string;categories?:string[];locationTitles?:string[];supportedBy?:{id:string;name:string}[];supportKnown?:boolean;catalogRevision?:number;catalogSource?:string;locationsLoaded?:boolean };
export type Entrance = {bossInstanced?:boolean;objectChunks?:Record<string,number[]>;overlay:string;direction:string;plane:string;objectType:string;ids:string[];chunks:number[];reviewed?:boolean};
export type Draft = { entranceSetupProgress?:number; bossInstanced?:boolean; setupProgress?:number; metadataBase?:EncounterMetadata;recommendedSeconds?:number|null;petIcon?:string; editFlow?:'fresh'; entranceDangerous?:boolean|null; entranceNotifyChunks?:number[]; entranceBaseRaw?:string|null; regionType?:string; id:string; name:string; regions:number[]; deathType:string; baseRaw:string|null; reviewed?:boolean; entrance?:Entrance; entranceOverlay?:string; chunks?:number[]; entranceRegion?:number; entrancePlane?:number };
export type Snapshot = { metadataSources?:Record<string,string>; version:number; generatedAt:string|null; baseCommit:string|null; source:string; entries:Boss[]; candidates:Boss[]; dungeons?:Boss[]; warnings:string[];catalog?:{source:string;revision:number;count:number;fetchedAt:string} };

export type EvidenceContexts = Record<string, { arena?: Location; entrance?: Location; entranceImage?: string|null }>;
