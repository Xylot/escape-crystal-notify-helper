export type Location = { x:number; y:number; region:number; plane:number|null; mapId:string|null; role:string; caption:string; source:string; title:string; revision:number|null; verified:boolean;pinX?:number;pinY?:number;icon?:string|null;mtype?:string|null;zoom?:number|null;markerSource?:string;tiles?:{version:string;mapId:number;plane:number} };
export type Boss = { id:string; name:string; regions:number[]; deathType:string; regionType?:string; raw:string|null; optionalArgs:string[]; maps:Location[]; warnings:string[]; links:string[]; wikiTitle:string; categories?:string[];locationTitles?:string[];supportedBy?:{id:string;name:string}[];supportKnown?:boolean;catalogRevision?:number;catalogSource?:string;locationsLoaded?:boolean };
export type Entrance = {overlay:string;direction:string;plane:string;objectType:string;ids:string[];chunks:number[];reviewed?:boolean};
export type Draft = { id:string; name:string; regions:number[]; deathType:string; baseRaw:string|null; reviewed?:boolean; entrance?:Entrance; chunks?:number[]; entranceRegion?:number; entrancePlane?:number };
export type Snapshot = { version:number; generatedAt:string|null; baseCommit:string|null; source:string; entries:Boss[]; candidates:Boss[]; warnings:string[];catalog?:{source:string;revision:number;count:number;fetchedAt:string} };
