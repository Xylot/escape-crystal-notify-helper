import type { Boss } from './types';
export default function Discovery({bosses,onSelect}:{bosses:Boss[];onSelect:(boss:Boss)=>void}){
 const featured=bosses.find(b=>b.wikiTitle.toLowerCase()==='shellbane gryphon');
 return <section className="discovery"><div className="discovery-intro"><div><div className="eyebrow">ESCAPE CRYSTAL · CONTENT DISCOVERY</div><h1>Give the next encounter<br/>an escape plan.</h1><p>Find a boss missing from the plugin. Explore its entrance and arena, select coverage, and prepare a contribution.</p></div><div className="crystal-diagram" aria-hidden="true"><span>◇</span><i>64 × 64 REGIONS</i></div></div>
 {featured&&<button className="featured-boss" onClick={()=>onSelect(featured)}><div><span className="eyebrow">READY TO EXPLORE · WIKI LOCATIONS AVAILABLE</span><h2>Shellbane gryphon</h2><p>From the Great Conch to the gryphon’s lair.<br/>Review the entrance and arena side by side.</p></div><span className="feature-action">Open encounter ↗</span></button>}
 <div className="discovery-title"><h2>Find your next contribution</h2><span>{bosses.length} matching encounters</span></div><div className="discovery-cards">{bosses.map(b=><button className="discovery-card" key={b.id} onClick={()=>onSelect(b)}><span className="eyebrow">{b.categories?.[0]??'Encounter'}</span><h3>{b.name}</h3><p>{b.raw||b.supportedBy?.length?'Supported · review existing coverage':b.maps.length?'Wiki locations ready':'Load wiki locations to begin'}</p><span className="card-bottom">{b.raw||b.supportedBy?.length?'Review encounter':'Explore coverage'} <b>↗</b></span></button>)}</div>{!bosses.length&&<p className="empty">No encounters match these filters. Try another category or include quest bosses.</p>}</section>;
}
